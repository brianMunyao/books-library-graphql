import styled from 'styled-components';

const Separator = () => {
	return <Container />;
};

const Container = styled.div`
	height: 1px;
	width: 90%;
	background: #e1e1e1;
	margin: 15px auto;
`;

export default Separator;
